---
import Layout from '../../../layouts/Layout.astro';
import ColabNav from '../../../components/colab/ColabNav.astro';
import '../../../styles/members.css';
import EvryluxLogoLoader from '../../../components/ui/EvryluxLogoLoader.astro';
---

<Layout
  title="Roadmap interno — EVRYLUX Colab"
  description="Roadmap interno detalhado do projeto EVRYLUX."
>
  <main class="member-shell roadmap-page">
    <EvryluxLogoLoader
      id="roadmap-loading"
      size="md"
    />

    <div
      id="roadmap-content"
      class="member-container member-hidden"
    >
      <div class="member-topbar">
        <div class="member-title-block">
          <div class="eyebrow">
            Studio / Roadmap
          </div>

          <h1>
            Roadmap interno
          </h1>

          <p>
            Veja todas as etapas do projeto em uma única visão.
          </p>
        </div>

        <ColabNav />
      </div>

      <div class="roadmap-toolbar">
        <div class="roadmap-filter-row">
          <button
            class="roadmap-filter active"
            type="button"
            data-filter="all"
          >
            Todos
          </button>

          <button
            class="roadmap-filter"
            type="button"
            data-filter="mine"
          >
            Meus itens
          </button>

          <button
            class="roadmap-filter"
            type="button"
            data-filter="in_progress"
          >
            Em andamento
          </button>

          <button
            class="roadmap-filter"
            type="button"
            data-filter="done"
          >
            Concluídos
          </button>
        </div>

        <a
          class="studio-validation-button"
          href="/colab/studio/validation"
          data-studio-validation-button
        >
          <span class="studio-validation-button-icon" aria-hidden="true">✓</span>
          <span>Validação</span>
        </a>

        <button
          id="new-roadmap-item"
          class="roadmap-new-button"
          type="button"
        >
          <span aria-hidden="true">＋</span>
          Novo item
        </button>
      </div>

      <div
        id="roadmap-error"
        class="roadmap-error member-hidden"
        role="status"
      ></div>

      <section
        class="roadmap-board"
        aria-label="Roadmap do projeto"
      >
        {
          [
            {
              id: 'now',
              kicker: 'Foco atual',
              title: 'Agora',
            },
            {
              id: 'next',
              kicker: 'Próximos passos',
              title: 'Próximo',
            },
            {
              id: 'later',
              kicker: 'Planejado',
              title: 'Depois',
            },
            {
              id: 'done',
              kicker: 'Histórico',
              title: 'Concluído',
            },
          ].map(
            (
              stage,
            ) => (
              <section class="roadmap-column">
                <div class="roadmap-column-head">
                  <div>
                    <span class="roadmap-column-kicker">
                      {stage.kicker}
                    </span>

                    <h2>
                      {stage.title}
                    </h2>
                  </div>

                  <span
                    id={`stage-count-${stage.id}`}
                    class="roadmap-column-count"
                  >
                    0
                  </span>
                </div>

                <div
                  id={`stage-${stage.id}`}
                  class="roadmap-column-list"
                ></div>
              </section>
            ),
          )
        }
      </section>
    </div>

    <!-- ===================================================== -->
    <!-- TEMPLATE DO CARD                                      -->
    <!-- ===================================================== -->

    <template id="roadmap-card-template">
      <article
        class="roadmap-item-card roadmap-item-collapsible"
        aria-expanded="false"
      >
        <div class="roadmap-card-summary">
          <div class="roadmap-card-title-row">
            <h3 data-role="title"></h3>

            <span
              class="roadmap-my-assignment-marker member-hidden"
              data-role="my-assignment-marker"
              title="Você está marcado nesta tarefa"
              aria-label="Você está marcado nesta tarefa"
              role="img"
            ></span>
          </div>

          <button
            class="roadmap-card-toggle"
            type="button"
            data-role="toggle-card"
            aria-label="Expandir item"
            aria-expanded="false"
            title="Expandir"
          >
            <svg
              viewBox="0 0 24 24"
              aria-hidden="true"
            >
              <path
                d="m7 10 5 5 5-5"
                fill="none"
                stroke="currentColor"
                stroke-width="1.8"
                stroke-linecap="round"
                stroke-linejoin="round"
              />
            </svg>
          </button>
        </div>

        <div
          class="roadmap-card-expanded"
          aria-hidden="true"
        >
          <div class="roadmap-card-badges">
            <span
              class="roadmap-pill roadmap-priority"
              data-role="priority"
            ></span>

            <span
              class="roadmap-pill roadmap-status"
              data-role="status"
            ></span>
          </div>

          <p
            class="roadmap-card-description"
            data-role="description"
          ></p>

          <div class="roadmap-card-assignee">
            <span
              class="roadmap-card-avatar"
              data-role="assignee-avatar"
            ></span>

            <div>
              <span>
                Responsáveis
              </span>

              <strong data-role="assignee"></strong>
            </div>
          </div>

          <div class="roadmap-card-progress">
            <div class="roadmap-card-progress-head">
              <span>
                Progresso
              </span>

              <strong data-role="progress-label">
                0%
              </strong>
            </div>

            <div class="roadmap-card-progress-track">
              <span
                data-role="progress-fill"
                class="roadmap-card-progress-fill"
              ></span>
            </div>
          </div>

          <div class="roadmap-validation-actions">
            <button
              class="roadmap-card-details-button"
              type="button"
              data-role="open-details"
            >
              Ver detalhes / editar
            </button>

            <button
              class="roadmap-card-validation-button"
              type="button"
              data-role="send-validation"
            >
              Enviar para validação
            </button>
          </div>
        </div>
      </article>
    </template>

    <!-- ===================================================== -->
    <!-- MODAL CRIAR / EDITAR                                  -->
    <!-- ===================================================== -->

    <div
      id="roadmap-modal"
      class="roadmap-modal member-hidden"
      role="dialog"
      aria-modal="true"
      aria-labelledby="roadmap-modal-title"
    >
      <button
        id="roadmap-modal-backdrop"
        class="roadmap-modal-backdrop"
        type="button"
        aria-label="Fechar"
      ></button>

      <section class="roadmap-modal-card">
        <div class="roadmap-modal-head">
          <div>
            <span class="roadmap-modal-kicker">
              Roadmap
            </span>

            <h2 id="roadmap-modal-title">
              Novo item
            </h2>
          </div>

          <button
            id="roadmap-modal-close"
            class="roadmap-modal-close"
            type="button"
            aria-label="Fechar"
          >
            ×
          </button>
        </div>

        <form
          id="roadmap-form"
          class="roadmap-form"
        >
          <input
            id="roadmap-item-id"
            type="hidden"
          />

          <div class="roadmap-main-grid">
            <div class="roadmap-field roadmap-title-field">
              <label for="roadmap-title">
                Título
              </label>

              <input
                id="roadmap-title"
                type="text"
                maxlength="140"
                required
                placeholder="O que precisa ser feito?"
              />
            </div>

            <div class="roadmap-field">
              <label for="roadmap-description">
                Descrição
              </label>

              <textarea
                id="roadmap-description"
                maxlength="2000"
                placeholder="Explique o objetivo deste item."
              ></textarea>
            </div>
          </div>

          <div class="roadmap-visual-grid">
            <div class="roadmap-visual-control">
              <div class="roadmap-control-head">
                <label for="roadmap-stage-range">
                  Etapa
                </label>

                <strong id="roadmap-stage-value">
                  Agora
                </strong>
              </div>

              <input
                id="roadmap-stage-range"
                class="roadmap-range"
                type="range"
                min="0"
                max="3"
                step="1"
                value="0"
              />

              <div class="roadmap-range-labels roadmap-stage-labels">
                <span>Agora</span>
                <span>Próximo</span>
                <span>Depois</span>
                <span>Concluído</span>
              </div>

              <select
                id="roadmap-stage"
                class="roadmap-hidden-select"
                tabindex="-1"
                aria-hidden="true"
              >
                <option value="now">Agora</option>
                <option value="next">Próximo</option>
                <option value="later">Depois</option>
                <option value="done">Concluído</option>
              </select>
            </div>

            <div class="roadmap-visual-control">
              <div class="roadmap-control-head">
                <label for="roadmap-priority-range">
                  Prioridade
                </label>

                <strong id="roadmap-priority-value">
                  Média
                </strong>
              </div>

              <input
                id="roadmap-priority-range"
                class="roadmap-range"
                type="range"
                min="0"
                max="2"
                step="1"
                value="1"
              />

              <div class="roadmap-range-labels roadmap-priority-labels">
                <span>Baixa</span>
                <span>Média</span>
                <span>Alta</span>
              </div>

              <select
                id="roadmap-priority"
                class="roadmap-hidden-select"
                tabindex="-1"
                aria-hidden="true"
              >
                <option value="low">Baixa</option>
                <option value="medium">Média</option>
                <option value="high">Alta</option>
              </select>
            </div>
          </div>

          <div class="roadmap-form-grid">
            <div class="roadmap-field">
              <label for="roadmap-status">
                Status
              </label>

              <select id="roadmap-status">
                <option value="todo">A fazer</option>
                <option value="in_progress">Em andamento</option>
                <option value="blocked">Bloqueado</option>
                <option value="done">Concluído</option>
              </select>
            </div>

            <div class="roadmap-field roadmap-assignees-field">
              <label>
                Responsáveis
              </label>

              <div
                id="roadmap-assignees-picker"
                class="roadmap-assignees-picker"
              >
                <button
                  id="roadmap-assignees-trigger"
                  class="roadmap-assignees-trigger"
                  type="button"
                  aria-expanded="false"
                >
                  <span id="roadmap-assignees-trigger-label">
                    Selecionar responsáveis
                  </span>

                  <span
                    class="roadmap-assignees-chevron"
                    aria-hidden="true"
                  >
                    ▾
                  </span>
                </button>

                <div
                  id="roadmap-assignees-menu"
                  class="roadmap-assignees-menu member-hidden"
                ></div>

                <div
                  id="roadmap-assignees-chips"
                  class="roadmap-assignees-chips"
                ></div>

                <!--
                  Mantido como campo primário para compatibilidade
                  com o Roadmap e a Validação já existentes.
                  O primeiro responsável selecionado é gravado aqui.
                -->
                <select
                  id="roadmap-assignee"
                  class="roadmap-primary-assignee-select"
                  tabindex="-1"
                  aria-hidden="true"
                >
                  <option value="">
                    Sem responsável
                  </option>
                </select>
              </div>
            </div>

            <div class="roadmap-field">
              <label for="roadmap-area">
                Área
              </label>

              <input
                id="roadmap-area"
                type="text"
                maxlength="120"
                placeholder="Ex.: Front e Backend"
              />
            </div>

            <div class="roadmap-field">
              <label for="roadmap-due-date">
                Prazo
              </label>

              <input
                id="roadmap-due-date"
                type="date"
              />
            </div>
          </div>

          <div class="roadmap-progress-row">
            <div class="roadmap-progress-field">
              <div class="roadmap-control-head">
                <label for="roadmap-progress">
                  Progresso
                </label>

                <strong id="roadmap-progress-value">
                  0%
                </strong>
              </div>

              <input
                id="roadmap-progress"
                class="roadmap-range"
                type="range"
                min="0"
                max="100"
                step="5"
                value="0"
              />
            </div>

            <div class="roadmap-field roadmap-notes-field">
              <label for="roadmap-notes">
                Notas internas
              </label>

              <textarea
                id="roadmap-notes"
                maxlength="3000"
                placeholder="Contexto adicional, decisões, links ou observações."
              ></textarea>
            </div>
          </div>

          <div
            id="roadmap-form-status"
            class="roadmap-form-status"
            role="status"
          ></div>

          <div class="roadmap-modal-actions">
            <button
              id="roadmap-delete"
              class="roadmap-button roadmap-button-danger member-hidden"
              type="button"
            >
              Excluir
            </button>

            <div class="roadmap-modal-actions-right">
              <button
                id="roadmap-cancel"
                class="roadmap-button roadmap-button-secondary"
                type="button"
              >
                Cancelar
              </button>

              <button
                id="roadmap-save"
                class="roadmap-button roadmap-button-primary"
                type="submit"
              >
                Criar item
              </button>
            </div>
          </div>
        </form>
      </section>
    </div>

    <!-- ===================================================== -->
    <!-- MODAL DE DETALHES                                     -->
    <!-- ===================================================== -->

    <div
      id="roadmap-details-modal"
      class="roadmap-details-modal member-hidden"
      role="dialog"
      aria-modal="true"
      aria-labelledby="roadmap-details-title"
    >
      <button
        id="roadmap-details-backdrop"
        class="roadmap-details-backdrop"
        type="button"
        aria-label="Fechar detalhes"
      ></button>

      <section class="roadmap-details-card">
        <div class="roadmap-details-head">
          <div class="roadmap-details-heading">
            <span class="roadmap-details-kicker">
              Item do roadmap
            </span>

            <div
              id="roadmap-details-badges"
              class="roadmap-details-badges"
            ></div>

            <h2 id="roadmap-details-title">
              Item
            </h2>

            <p id="roadmap-details-description">
              —
            </p>
          </div>

          <button
            id="roadmap-details-close"
            class="roadmap-details-close"
            type="button"
            aria-label="Fechar"
          >
            ×
          </button>
        </div>

        <div class="roadmap-details-grid">
          <div class="roadmap-details-info">
            <span
              id="roadmap-details-avatar"
              class="roadmap-details-avatar"
            ></span>

            <div>
              <span>
                Responsáveis
              </span>

              <strong id="roadmap-details-assignee">
                —
              </strong>
            </div>
          </div>

          <div class="roadmap-details-info">
            <div>
              <span>
                Área
              </span>

              <strong id="roadmap-details-area">
                —
              </strong>
            </div>
          </div>

          <div class="roadmap-details-info">
            <div>
              <span>
                Prazo
              </span>

              <strong id="roadmap-details-due-date">
                —
              </strong>
            </div>
          </div>

          <div class="roadmap-details-info">
            <div>
              <span>
                Etapa
              </span>

              <strong id="roadmap-details-stage">
                —
              </strong>
            </div>
          </div>
        </div>

        <div class="roadmap-details-progress">
          <div class="roadmap-details-progress-head">
            <span>
              Progresso
            </span>

            <strong id="roadmap-details-progress-label">
              0%
            </strong>
          </div>

          <div class="roadmap-details-progress-track">
            <span id="roadmap-details-progress-fill"></span>
          </div>
        </div>

        <div
          id="roadmap-details-notes-wrap"
          class="roadmap-details-notes member-hidden"
        >
          <span>
            Notas internas
          </span>

          <p id="roadmap-details-notes"></p>
        </div>

        <div class="roadmap-details-actions">
          <button
            id="roadmap-details-edit"
            class="roadmap-button roadmap-button-primary"
            type="button"
          >
            Editar item
          </button>
        </div>
      </section>
    </div>
  </main>
</Layout>


<script>
  import {
    requireMemberSession,
  } from '../../../lib/member-auth';

  import {
    createRoadmapItem,
    deleteRoadmapItem,
    getCollaboratorProfiles,
    getRoadmapItems,
    priorityLabels,
    stageLabels,
    statusLabels,
    updateRoadmapItem,
    type CollaboratorProfile,
    type RoadmapItem,
    type RoadmapItemInput,
    type RoadmapPriority,
    type RoadmapStage,
    type RoadmapStatus,
  } from '../../../lib/colab-roadmap';

  import {
    getRoadmapAssigneeMap,
    setRoadmapAssignees,
    type RoadmapAssigneeMap,
  } from '../../../lib/colab-roadmap-assignees';

  import {
    getValidationItems,
    sendRoadmapToValidation,
    type ValidationItem,
  } from '../../../lib/colab-validation';

  const session =
    await requireMemberSession();

  const loading =
    document.querySelector<HTMLElement>('#roadmap-loading');

  const content =
    document.querySelector<HTMLElement>('#roadmap-content');

  const errorBox =
    document.querySelector<HTMLElement>('#roadmap-error');

  const newButton =
    document.querySelector<HTMLButtonElement>('#new-roadmap-item');

  const cardTemplate =
    document.querySelector<HTMLTemplateElement>('#roadmap-card-template');

  const modal =
    document.querySelector<HTMLElement>('#roadmap-modal');

  const modalBackdrop =
    document.querySelector<HTMLButtonElement>('#roadmap-modal-backdrop');

  const modalClose =
    document.querySelector<HTMLButtonElement>('#roadmap-modal-close');

  const modalCancel =
    document.querySelector<HTMLButtonElement>('#roadmap-cancel');

  const modalTitle =
    document.querySelector<HTMLElement>('#roadmap-modal-title');

  const form =
    document.querySelector<HTMLFormElement>('#roadmap-form');

  const itemId =
    document.querySelector<HTMLInputElement>('#roadmap-item-id');

  const titleInput =
    document.querySelector<HTMLInputElement>('#roadmap-title');

  const descriptionInput =
    document.querySelector<HTMLTextAreaElement>('#roadmap-description');

  const stageInput =
    document.querySelector<HTMLSelectElement>('#roadmap-stage');

  const stageRange =
    document.querySelector<HTMLInputElement>('#roadmap-stage-range');

  const stageValue =
    document.querySelector<HTMLElement>('#roadmap-stage-value');

  const statusInput =
    document.querySelector<HTMLSelectElement>('#roadmap-status');

  const priorityInput =
    document.querySelector<HTMLSelectElement>('#roadmap-priority');

  const priorityRange =
    document.querySelector<HTMLInputElement>('#roadmap-priority-range');

  const priorityValue =
    document.querySelector<HTMLElement>('#roadmap-priority-value');

  const assigneeInput =
    document.querySelector<HTMLSelectElement>('#roadmap-assignee');


  const assigneesPicker =
    document.querySelector<HTMLElement>('#roadmap-assignees-picker');

  const assigneesTrigger =
    document.querySelector<HTMLButtonElement>('#roadmap-assignees-trigger');

  const assigneesTriggerLabel =
    document.querySelector<HTMLElement>('#roadmap-assignees-trigger-label');

  const assigneesMenu =
    document.querySelector<HTMLElement>('#roadmap-assignees-menu');

  const assigneesChips =
    document.querySelector<HTMLElement>('#roadmap-assignees-chips');

  const areaInput =
    document.querySelector<HTMLInputElement>('#roadmap-area');

  const dueDateInput =
    document.querySelector<HTMLInputElement>('#roadmap-due-date');

  const progressInput =
    document.querySelector<HTMLInputElement>('#roadmap-progress');

  const progressValue =
    document.querySelector<HTMLElement>('#roadmap-progress-value');

  const notesInput =
    document.querySelector<HTMLTextAreaElement>('#roadmap-notes');

  const saveButton =
    document.querySelector<HTMLButtonElement>('#roadmap-save');

  const deleteButton =
    document.querySelector<HTMLButtonElement>('#roadmap-delete');

  const formStatus =
    document.querySelector<HTMLElement>('#roadmap-form-status');

  const filterButtons =
    Array.from(
      document.querySelectorAll<HTMLButtonElement>(
        '.roadmap-filter',
      ),
    );

  const detailsModal =
    document.querySelector<HTMLElement>('#roadmap-details-modal');

  const detailsBackdrop =
    document.querySelector<HTMLButtonElement>('#roadmap-details-backdrop');

  const detailsClose =
    document.querySelector<HTMLButtonElement>('#roadmap-details-close');

  const detailsEdit =
    document.querySelector<HTMLButtonElement>('#roadmap-details-edit');

  const detailsTitle =
    document.querySelector<HTMLElement>('#roadmap-details-title');

  const detailsDescription =
    document.querySelector<HTMLElement>('#roadmap-details-description');

  const detailsBadges =
    document.querySelector<HTMLElement>('#roadmap-details-badges');

  const detailsAvatar =
    document.querySelector<HTMLElement>('#roadmap-details-avatar');

  const detailsAssignee =
    document.querySelector<HTMLElement>('#roadmap-details-assignee');

  const detailsArea =
    document.querySelector<HTMLElement>('#roadmap-details-area');

  const detailsDueDate =
    document.querySelector<HTMLElement>('#roadmap-details-due-date');

  const detailsStage =
    document.querySelector<HTMLElement>('#roadmap-details-stage');

  const detailsProgressLabel =
    document.querySelector<HTMLElement>('#roadmap-details-progress-label');

  const detailsProgressFill =
    document.querySelector<HTMLElement>('#roadmap-details-progress-fill');

  const detailsNotesWrap =
    document.querySelector<HTMLElement>('#roadmap-details-notes-wrap');

  const detailsNotes =
    document.querySelector<HTMLElement>('#roadmap-details-notes');

  const stages:
    RoadmapStage[] = [
      'now',
      'next',
      'later',
      'done',
    ];

  const stageRangeMap:
    RoadmapStage[] = [
      'now',
      'next',
      'later',
      'done',
    ];

  const priorityRangeMap:
    RoadmapPriority[] = [
      'low',
      'medium',
      'high',
    ];

  let roadmapItems:
    RoadmapItem[] =
      [];

  let collaborators:
    CollaboratorProfile[] =
      [];

  let validationItems:
    ValidationItem[] =
      [];


  let roadmapAssigneeMap:
    RoadmapAssigneeMap =
      {};

  let selectedAssigneeIds:
    string[] =
      [];

  let activeFilter:
    'all' |
    'mine' |
    'in_progress' |
    'done' =
      'all';

  let detailsItem:
    RoadmapItem | null =
      null;

  function syncStageVisualFromRange() {
    if (
      !stageRange ||
      !stageInput ||
      !stageValue
    ) {
      return;
    }

    const index =
      Math.max(
        0,
        Math.min(
          3,
          Number(stageRange.value),
        ),
      );

    const stage =
      stageRangeMap[index];

    stageInput.value =
      stage;

    stageValue.textContent =
      stageLabels[stage];
  }

  function syncStageVisualFromSelect() {
    if (
      !stageRange ||
      !stageInput ||
      !stageValue
    ) {
      return;
    }

    const stage =
      stageInput.value as RoadmapStage;

    const index =
      Math.max(
        0,
        stageRangeMap.indexOf(stage),
      );

    stageRange.value =
      String(index);

    stageValue.textContent =
      stageLabels[stage];
  }

  function syncPriorityVisualFromRange() {
    if (
      !priorityRange ||
      !priorityInput ||
      !priorityValue
    ) {
      return;
    }

    const index =
      Math.max(
        0,
        Math.min(
          2,
          Number(priorityRange.value),
        ),
      );

    const priority =
      priorityRangeMap[index];

    priorityInput.value =
      priority;

    priorityValue.textContent =
      priorityLabels[priority];
  }

  function syncPriorityVisualFromSelect() {
    if (
      !priorityRange ||
      !priorityInput ||
      !priorityValue
    ) {
      return;
    }

    const priority =
      priorityInput.value as RoadmapPriority;

    const index =
      Math.max(
        0,
        priorityRangeMap.indexOf(priority),
      );

    priorityRange.value =
      String(index);

    priorityValue.textContent =
      priorityLabels[priority];
  }

  function showError(
    message: string,
  ) {
    if (!errorBox) {
      return;
    }

    errorBox.textContent =
      message;

    errorBox.classList.remove(
      'member-hidden',
    );
  }

  function clearError() {
    errorBox?.classList.add(
      'member-hidden',
    );
  }

  function showFormStatus(
    message: string,
    type:
      'success' |
      'error' |
      '',
  ) {
    if (!formStatus) {
      return;
    }

    formStatus.textContent =
      message;

    formStatus.className =
      type
        ? `roadmap-form-status ${type}`
        : 'roadmap-form-status';
  }



  function renderRoadmapAvatar(
    target: HTMLElement | null,
    profile: CollaboratorProfile | null,
    name: string,
  ) {
    if (!target) {
      return;
    }

    target.innerHTML = '';
    target.style.backgroundImage = '';
    target.classList.add('has-image');

    const fallback =
      '/branding/default-profile-avatar.png';

    const image =
      document.createElement('img');

    image.className =
      'roadmap-profile-avatar-image';

    image.src =
      profile?.avatar_url?.trim() ||
      fallback;

    image.alt =
      `Foto de ${name}`;

    image.loading =
      'lazy';

    image.draggable =
      false;

    image.addEventListener(
      'error',
      () => {
        const current =
          image.getAttribute('src') ||
          '';

        if (
          !current.endsWith(
            fallback,
          )
        ) {
          image.src =
            fallback;
        }
      },
    );

    target.append(image);
  }

  function formatDate(
    value: string | null,
  ) {
    if (!value) {
      return 'Sem prazo';
    }

    return new Date(
      `${value}T12:00:00`,
    ).toLocaleDateString(
      'pt-BR',
    );
  }

  function collaboratorFor(
    userId: string | null,
  ) {
    if (!userId) {
      return null;
    }

    return (
      collaborators.find(
        (profile) =>
          profile.user_id ===
          userId,
      ) ??
      null
    );
  }

  function collaboratorName(
    userId: string | null,
  ) {
    const profile =
      collaboratorFor(
        userId,
      );

    if (!profile) {
      return 'Sem responsável';
    }

    return (
      profile.display_name?.trim() ||
      profile.github_login?.trim() ||
      'Colaborador EVRYLUX'
    );
  }


  function assigneeIdsForItem(
    item: RoadmapItem,
  ) {
    const ids =
      roadmapAssigneeMap[item.id] ??
      [];

    if (
      ids.length >
      0
    ) {
      return ids;
    }

    return item.assignee_user_id
      ? [item.assignee_user_id]
      : [];
  }

  function assigneeNamesForItem(
    item: RoadmapItem,
  ) {
    const ids =
      assigneeIdsForItem(
        item,
      );

    if (
      ids.length ===
      0
    ) {
      return 'Sem responsável';
    }

    return ids
      .map(
        (
          id,
        ) =>
          collaboratorName(
            id,
          ),
      )
      .join(', ');
  }

  function renderRoadmapAvatarStack(
    target: HTMLElement | null,
    userIds: string[],
  ) {
    if (!target) {
      return;
    }

    target.innerHTML =
      '';

    target.classList.add(
      'roadmap-avatar-stack',
    );

    const ids =
      userIds.slice(
        0,
        3,
      );

    if (
      ids.length ===
      0
    ) {
      renderRoadmapAvatar(
        target,
        null,
        'Sem responsável',
      );

      return;
    }

    ids.forEach(
      (
        userId,
        index,
      ) => {
        const profile =
          collaboratorFor(
            userId,
          );

        const name =
          collaboratorName(
            userId,
          );

        const item =
          document.createElement(
            'span',
          );

        item.className =
          'roadmap-stacked-avatar';

        item.style.zIndex =
          String(
            10 -
            index,
          );

        renderRoadmapAvatar(
          item,
          profile,
          name,
        );

        target.appendChild(
          item,
        );
      },
    );

    if (
      userIds.length >
      3
    ) {
      const more =
        document.createElement(
          'span',
        );

      more.className =
        'roadmap-stacked-avatar roadmap-stacked-avatar-more';

      more.textContent =
        `+${userIds.length - 3}`;

      target.appendChild(
        more,
      );
    }
  }

  function syncPrimaryAssignee() {
    if (!assigneeInput) {
      return;
    }

    assigneeInput.value =
      selectedAssigneeIds[0] ??
      '';
  }

  function renderAssigneePicker() {
    if (
      !assigneesMenu ||
      !assigneesChips
    ) {
      return;
    }

    assigneesMenu.innerHTML =
      '';

    if (
      collaborators.length ===
      0
    ) {
      const empty =
        document.createElement(
          'div',
        );

      empty.className =
        'roadmap-assignees-empty';

      empty.textContent =
        'Nenhum colaborador disponível.';

      assigneesMenu.appendChild(
        empty,
      );
    }

    collaborators.forEach(
      (
        profile,
      ) => {
        const selected =
          selectedAssigneeIds.includes(
            profile.user_id,
          );

        const option =
          document.createElement(
            'button',
          );

        option.type =
          'button';

        option.className =
          `roadmap-assignee-option${selected ? ' selected' : ''}`;

        option.dataset.userId =
          profile.user_id;

        const avatar =
          document.createElement(
            'span',
          );

        avatar.className =
          'roadmap-assignee-option-avatar';

        renderRoadmapAvatar(
          avatar,
          profile,
          profile.display_name?.trim() ||
            profile.github_login?.trim() ||
            'Colaborador EVRYLUX',
        );

        const copy =
          document.createElement(
            'span',
          );

        copy.className =
          'roadmap-assignee-option-copy';

        const strong =
          document.createElement(
            'strong',
          );

        strong.textContent =
          profile.display_name?.trim() ||
          profile.github_login?.trim() ||
          'Colaborador EVRYLUX';

        const small =
          document.createElement(
            'small',
          );

        small.textContent =
          profile.github_login?.trim() ||
          profile.area?.trim() ||
          'Colaborador';

        copy.append(
          strong,
          small,
        );

        const check =
          document.createElement(
            'span',
          );

        check.className =
          'roadmap-assignee-option-check';

        check.textContent =
          selected
            ? '✓'
            : '';

        option.append(
          avatar,
          copy,
          check,
        );

        option.addEventListener(
          'click',
          () => {
            const exists =
              selectedAssigneeIds.includes(
                profile.user_id,
              );

            selectedAssigneeIds =
              exists
                ? selectedAssigneeIds.filter(
                    (
                      id,
                    ) =>
                      id !==
                      profile.user_id,
                  )
                : [
                    ...selectedAssigneeIds,
                    profile.user_id,
                  ];

            syncPrimaryAssignee();
            renderAssigneePicker();
          },
        );

        assigneesMenu.appendChild(
          option,
        );
      },
    );

    assigneesChips.innerHTML =
      '';

    selectedAssigneeIds.forEach(
      (
        userId,
      ) => {
        const profile =
          collaboratorFor(
            userId,
          );

        const chip =
          document.createElement(
            'button',
          );

        chip.type =
          'button';

        chip.className =
          'roadmap-assignee-chip';

        chip.title =
          'Remover responsável';

        const label =
          profile?.display_name?.trim() ||
          profile?.github_login?.trim() ||
          'Colaborador';

        chip.innerHTML =
          `<span>${label}</span><b aria-hidden="true">×</b>`;

        chip.addEventListener(
          'click',
          () => {
            selectedAssigneeIds =
              selectedAssigneeIds.filter(
                (
                  id,
                ) =>
                  id !==
                  userId,
              );

            syncPrimaryAssignee();
            renderAssigneePicker();
          },
        );

        assigneesChips.appendChild(
          chip,
        );
      },
    );

    if (
      assigneesTriggerLabel
    ) {
      assigneesTriggerLabel.textContent =
        selectedAssigneeIds.length ===
        0
          ? 'Selecionar responsáveis'
          : selectedAssigneeIds.length ===
              1
            ? collaboratorName(
                selectedAssigneeIds[0],
              )
            : `${selectedAssigneeIds.length} responsáveis selecionados`;
    }
  }

  function openAssigneePicker() {
    if (
      !assigneesMenu ||
      !assigneesTrigger
    ) {
      return;
    }

    const opening =
      assigneesMenu.classList.contains(
        'member-hidden',
      );

    assigneesMenu.classList.toggle(
      'member-hidden',
      !opening,
    );

    assigneesTrigger.setAttribute(
      'aria-expanded',
      String(opening),
    );
  }

  function closeAssigneePicker() {
    assigneesMenu?.classList.add(
      'member-hidden',
    );

    assigneesTrigger?.setAttribute(
      'aria-expanded',
      'false',
    );
  }

  function filteredItems() {
    if (!session) {
      return roadmapItems;
    }

    if (activeFilter === 'mine') {
      return roadmapItems.filter(
        (item) =>
          assigneeIdsForItem(
            item,
          ).includes(
            session.user.id,
          ),
      );
    }

    if (
      activeFilter ===
      'in_progress'
    ) {
      return roadmapItems.filter(
        (item) =>
          item.status ===
          'in_progress',
      );
    }

    if (activeFilter === 'done') {
      return roadmapItems.filter(
        (item) =>
          item.status === 'done' ||
          item.stage === 'done',
      );
    }

    return roadmapItems;
  }

  function createEmpty(
    stage: RoadmapStage,
  ) {
    const empty =
      document.createElement(
        'div',
      );

    empty.className =
      'roadmap-empty';

    const strong =
      document.createElement(
        'strong',
      );

    strong.textContent =
      `Nenhum item em ${stageLabels[stage]}.`;

    const span =
      document.createElement(
        'span',
      );

    span.textContent =
      activeFilter === 'all'
        ? 'Crie um item para começar.'
        : 'Nenhum item corresponde ao filtro atual.';

    empty.append(
      strong,
      span,
    );

    return empty;
  }

  function createCard(
    item: RoadmapItem,
  ) {
    if (!cardTemplate) {
      return null;
    }

    const fragment =
      cardTemplate.content.cloneNode(
        true,
      ) as DocumentFragment;

    const card =
      fragment.querySelector<HTMLElement>(
        '.roadmap-item-card',
      );

    if (!card) {
      return null;
    }

    const assigneeIds =
      assigneeIdsForItem(
        item,
      );

    const myAssignmentMarker =
      card.querySelector<HTMLElement>(
        '[data-role="my-assignment-marker"]',
      );

    const isAssignedToMe =
      Boolean(
        session?.user?.id,
      ) &&
      assigneeIds.includes(
        session.user.id,
      );

    myAssignmentMarker?.classList.toggle(
      'member-hidden',
      !isAssignedToMe,
    );

    if (
      myAssignmentMarker
    ) {
      myAssignmentMarker.setAttribute(
        'aria-hidden',
        String(
          !isAssignedToMe,
        ),
      );
    }

    const profile =
      assigneeIds.length >
      0
        ? collaboratorFor(
            assigneeIds[0],
          )
        : null;

    const name =
      assigneeNamesForItem(
        item,
      );

    const setText = (
      selector: string,
      value: string,
    ) => {
      const element =
        card.querySelector<HTMLElement>(
          selector,
        );

      if (element) {
        element.textContent =
          value;
      }

      return element;
    };

    setText(
      '[data-role="title"]',
      item.title,
    );

    // EVRYLUX_ROADMAP_MARKER_ROBUST_V1
    const roadmapTitleElement =
      card.querySelector<HTMLElement>(
        '[data-role="title"]',
      );

    roadmapTitleElement?.classList.toggle(
      'roadmap-title-assigned-to-me',
      isAssignedToMe,
    );

    setText(
      '[data-role="description"]',
      item.description?.trim() ||
        'Sem descrição.',
    );

    const priority =
      setText(
        '[data-role="priority"]',
        priorityLabels[item.priority],
      );

    if (priority) {
      priority.dataset.priority =
        item.priority;
    }

    const status =
      setText(
        '[data-role="status"]',
        statusLabels[item.status],
      );

    if (status) {
      status.dataset.status =
        item.status;
    }

    setText(
      '[data-role="assignee"]',
      name,
    );

    setText(
      '[data-role="progress-label"]',
      `${item.progress}%`,
    );

    const avatar =
      card.querySelector<HTMLElement>(
        '[data-role="assignee-avatar"]',
      );

    renderRoadmapAvatarStack(
      avatar,
      assigneeIds,
    );

    const fill =
      card.querySelector<HTMLElement>(
        '[data-role="progress-fill"]',
      );

    if (fill) {
      fill.style.width =
        `${item.progress}%`;
    }

    const expandedBody =
      card.querySelector<HTMLElement>(
        '.roadmap-card-expanded',
      );

    const toggleButton =
      card.querySelector<HTMLButtonElement>(
        '[data-role="toggle-card"]',
      );

    // O item começa recolhido para ocupar o mínimo de espaço.
    // O usuário expande somente quando quiser ver os detalhes.
    card.classList.remove(
      'is-expanded',
    );

    card.setAttribute(
      'aria-expanded',
      'false',
    );

    expandedBody?.setAttribute(
      'aria-hidden',
      'true',
    );

    if (
      toggleButton
    ) {
      toggleButton.setAttribute(
        'aria-expanded',
        'false',
      );

      toggleButton.setAttribute(
        'aria-label',
        'Expandir item',
      );

      toggleButton.title =
        'Expandir';
    }

    const setExpanded =
      (
        expanded: boolean,
      ) => {
        card.classList.toggle(
          'is-expanded',
          expanded,
        );

        card.setAttribute(
          'aria-expanded',
          String(expanded),
        );

        expandedBody?.setAttribute(
          'aria-hidden',
          String(!expanded),
        );

        if (
          toggleButton
        ) {
          toggleButton.setAttribute(
            'aria-expanded',
            String(expanded),
          );

          toggleButton.setAttribute(
            'aria-label',
            expanded
              ? 'Encolher item'
              : 'Expandir item',
          );

          toggleButton.title =
            expanded
              ? 'Encolher'
              : 'Expandir';
        }
      };

    toggleButton?.addEventListener(
      'click',
      (
        event,
      ) => {
        event.stopPropagation();

        setExpanded(
          !card.classList.contains(
            'is-expanded',
          ),
        );
      },
    );

    const detailsButton =
      card.querySelector<HTMLButtonElement>(
        '[data-role="open-details"]',
      );

    detailsButton?.addEventListener(
      'click',
      (event) => {
        event.stopPropagation();

        openDetailsModal(
          item,
        );
      },
    );

    const validationButton =
      card.querySelector<HTMLButtonElement>(
        '[data-role="send-validation"]',
      );

    const alreadyInValidation =
      validationItems.some(
        (
          validation,
        ) =>
          validation.roadmap_item_id ===
          item.id,
      );

    if (
      validationButton &&
      alreadyInValidation
    ) {
      validationButton.textContent =
        'Em validação →';

      validationButton.classList.add(
        'already-sent',
      );
    }

    validationButton?.addEventListener(
      'click',
      async (
        event,
      ) => {
        event.stopPropagation();

        if (
          validationItems.some(
            (
              validation,
            ) =>
              validation.roadmap_item_id ===
              item.id,
          )
        ) {
          window.location.href =
            '/colab/studio/validation';

          return;
        }

        validationButton.disabled =
          true;

        validationButton.textContent =
          'Enviando...';

        try {
          await sendRoadmapToValidation(
            {
              id: item.id,
              title: item.title,
              description: item.description,
              area: item.area,
              assignee_user_id: item.assignee_user_id,
            },
          );

          validationItems =
            await getValidationItems();

          validationButton.textContent =
            'Em validação →';

          validationButton.classList.add(
            'already-sent',
          );

          validationButton.disabled =
            false;
        } catch (
          error
        ) {
          console.error(
            '[EVRYLUX] Erro ao enviar para validação:',
            error,
          );

          validationButton.textContent =
            'Tentar novamente';

          validationButton.disabled =
            false;
        }
      },
    );

    return card;
  }

  function renderRoadmap() {
    const items =
      filteredItems();

    stages.forEach(
      (stage) => {
        const container =
          document.querySelector<HTMLElement>(
            `#stage-${stage}`,
          );

        const count =
          document.querySelector<HTMLElement>(
            `#stage-count-${stage}`,
          );

        if (!container) {
          return;
        }

        const stageItems =
          items.filter(
            (item) =>
              item.stage ===
              stage,
          );

        if (count) {
          count.textContent =
            String(
              stageItems.length,
            );
        }

        container.innerHTML =
          '';

        if (!stageItems.length) {
          container.appendChild(
            createEmpty(
              stage,
            ),
          );

          return;
        }

        stageItems.forEach(
          (item) => {
            const card =
              createCard(
                item,
              );

            if (card) {
              container.appendChild(
                card,
              );
            }
          },
        );
      },
    );
  }

  function fillCollaborators() {
    if (!assigneeInput) {
      return;
    }

    assigneeInput.innerHTML =
      '';

    const emptyOption =
      document.createElement(
        'option',
      );

    emptyOption.value = '';
    emptyOption.textContent =
      'Sem responsável';

    assigneeInput.appendChild(
      emptyOption,
    );

    collaborators.forEach(
      (profile) => {
        const option =
          document.createElement(
            'option',
          );

        option.value =
          profile.user_id;

        option.textContent =
          profile.display_name?.trim() ||
          profile.github_login?.trim() ||
          'Colaborador EVRYLUX';

        assigneeInput.appendChild(
          option,
        );
      },
    );

    syncPrimaryAssignee();
    renderAssigneePicker();
  }

  function syncProgress() {
    if (
      progressInput &&
      progressValue
    ) {
      progressValue.textContent =
        `${progressInput.value}%`;
    }
  }

  function resetForm() {
    form?.reset();

    selectedAssigneeIds =
      [];

    syncPrimaryAssignee();
    renderAssigneePicker();
    closeAssigneePicker();

    if (itemId) {
      itemId.value =
        '';
    }

    if (stageInput) {
      stageInput.value =
        'now';
    }

    if (statusInput) {
      statusInput.value =
        'todo';
    }

    if (priorityInput) {
      priorityInput.value =
        'medium';
    }

    if (progressInput) {
      progressInput.value =
        '0';
    }

    syncStageVisualFromSelect();
    syncPriorityVisualFromSelect();
    syncProgress();

    showFormStatus(
      '',
      '',
    );

    deleteButton?.classList.add(
      'member-hidden',
    );

    if (saveButton) {
      saveButton.textContent =
        'Criar item';
    }
  }

  function openCreateModal() {
    resetForm();

    if (modalTitle) {
      modalTitle.textContent =
        'Novo item';
    }

    modal?.classList.remove(
      'member-hidden',
    );

    document.body.classList.add(
      'roadmap-modal-open',
    );

    titleInput?.focus();
  }

  function openEditModal(
    item: RoadmapItem,
  ) {
    resetForm();

    if (modalTitle) {
      modalTitle.textContent =
        'Editar item';
    }

    if (itemId) {
      itemId.value =
        item.id;
    }

    if (titleInput) {
      titleInput.value =
        item.title;
    }

    if (descriptionInput) {
      descriptionInput.value =
        item.description ??
        '';
    }

    if (stageInput) {
      stageInput.value =
        item.stage;
    }

    if (statusInput) {
      statusInput.value =
        item.status;
    }

    if (priorityInput) {
      priorityInput.value =
        item.priority;
    }

    selectedAssigneeIds =
      [
        ...assigneeIdsForItem(
          item,
        ),
      ];

    syncPrimaryAssignee();
    renderAssigneePicker();

    if (areaInput) {
      areaInput.value =
        item.area ??
        '';
    }

    if (dueDateInput) {
      dueDateInput.value =
        item.due_date ??
        '';
    }

    if (progressInput) {
      progressInput.value =
        String(
          item.progress,
        );
    }

    if (notesInput) {
      notesInput.value =
        item.notes ??
        '';
    }

    syncStageVisualFromSelect();
    syncPriorityVisualFromSelect();
    syncProgress();

    deleteButton?.classList.remove(
      'member-hidden',
    );

    if (saveButton) {
      saveButton.textContent =
        'Salvar alterações';
    }

    modal?.classList.remove(
      'member-hidden',
    );

    document.body.classList.add(
      'roadmap-modal-open',
    );
  }

  function closeModal() {
    modal?.classList.add(
      'member-hidden',
    );

    document.body.classList.remove(
      'roadmap-modal-open',
    );

    resetForm();
  }

  function openDetailsModal(
    item: RoadmapItem,
  ) {
    detailsItem =
      item;

    const assigneeIds =
      assigneeIdsForItem(
        item,
      );

    const profile =
      assigneeIds.length >
      0
        ? collaboratorFor(
            assigneeIds[0],
          )
        : null;

    const name =
      assigneeNamesForItem(
        item,
      );

    if (detailsTitle) {
      detailsTitle.textContent =
        item.title;
    }

    if (detailsDescription) {
      detailsDescription.textContent =
        item.description?.trim() ||
        'Sem descrição.';
    }

    if (detailsBadges) {
      detailsBadges.innerHTML =
        '';

      const priority =
        document.createElement(
          'span',
        );

      priority.className =
        'roadmap-pill roadmap-priority';

      priority.dataset.priority =
        item.priority;

      priority.textContent =
        priorityLabels[item.priority];

      const status =
        document.createElement(
          'span',
        );

      status.className =
        'roadmap-pill roadmap-status';

      status.dataset.status =
        item.status;

      status.textContent =
        statusLabels[item.status];

      detailsBadges.append(
        priority,
        status,
      );
    }

    renderRoadmapAvatarStack(
      detailsAvatar,
      assigneeIds,
    );

    if (detailsAssignee) {
      detailsAssignee.textContent =
        name;
    }

    if (detailsArea) {
      detailsArea.textContent =
        item.area?.trim() ||
        profile?.area?.trim() ||
        'Não informada';
    }

    if (detailsDueDate) {
      detailsDueDate.textContent =
        formatDate(
          item.due_date,
        );
    }

    if (detailsStage) {
      detailsStage.textContent =
        stageLabels[item.stage];
    }

    if (detailsProgressLabel) {
      detailsProgressLabel.textContent =
        `${item.progress}%`;
    }

    if (detailsProgressFill) {
      detailsProgressFill.style.width =
        `${item.progress}%`;
    }

    if (
      detailsNotesWrap &&
      detailsNotes
    ) {
      if (item.notes?.trim()) {
        detailsNotes.textContent =
          item.notes;

        detailsNotesWrap.classList.remove(
          'member-hidden',
        );
      } else {
        detailsNotes.textContent =
          '';

        detailsNotesWrap.classList.add(
          'member-hidden',
        );
      }
    }

    detailsModal?.classList.remove(
      'member-hidden',
    );

    document.body.classList.add(
      'roadmap-details-open',
    );

    detailsClose?.focus();
  }

  function closeDetailsModal() {
    detailsModal?.classList.add(
      'member-hidden',
    );

    document.body.classList.remove(
      'roadmap-details-open',
    );

    detailsItem =
      null;
  }

  function getFormInput():
    RoadmapItemInput {
    const rawProgress =
      Math.min(
        100,
        Math.max(
          0,
          Number(
            progressInput?.value ??
            0,
          ),
        ),
      );

    let stage =
      (
        stageInput?.value ??
        'now'
      ) as RoadmapStage;

    let status =
      (
        statusInput?.value ??
        'todo'
      ) as RoadmapStatus;

    if (
      stage === 'done' ||
      status === 'done'
    ) {
      stage =
        'done';

      status =
        'done';
    }

    return {
      title:
        titleInput?.value.trim() ??
        '',

      description:
        descriptionInput?.value.trim() ||
        null,

      stage,

      status,

      priority:
        (
          priorityInput?.value ??
          'medium'
        ) as RoadmapPriority,

      assignee_user_id:
        selectedAssigneeIds[0] ??
        null,

      area:
        areaInput?.value.trim() ||
        null,

      progress:
        status === 'done'
          ? 100
          : rawProgress,

      due_date:
        dueDateInput?.value ||
        null,

      notes:
        notesInput?.value.trim() ||
        null,
    };
  }

  async function loadRoadmap() {
    clearError();

    try {
      roadmapItems =
        await getRoadmapItems();
    } catch (error) {
      console.error(
        '[EVRYLUX] Erro ao carregar roadmap:',
        error,
      );

      showError(
        'Não foi possível carregar os itens do roadmap.',
      );

      roadmapItems =
        [];
    }

    try {
      collaborators =
        await getCollaboratorProfiles();

      fillCollaborators();
    } catch (error) {
      console.warn(
        '[EVRYLUX] Não foi possível carregar colaboradores:',
        error,
      );

      collaborators =
        [];

      fillCollaborators();
    }

    try {
      roadmapAssigneeMap =
        await getRoadmapAssigneeMap();
    } catch (
      error
    ) {
      console.warn(
        '[EVRYLUX] Não foi possível carregar múltiplos responsáveis:',
        error,
      );

      roadmapAssigneeMap =
        {};
    }

    try {
      validationItems =
        await getValidationItems();
    } catch (
      error
    ) {
      console.warn(
        '[EVRYLUX] Não foi possível carregar validações:',
        error,
      );

      validationItems =
        [];
    }

    renderRoadmap();
  }

  stageRange?.addEventListener(
    'input',
    syncStageVisualFromRange,
  );

  priorityRange?.addEventListener(
    'input',
    syncPriorityVisualFromRange,
  );

  assigneesTrigger?.addEventListener(
    'click',
    (
      event,
    ) => {
      event.stopPropagation();
      openAssigneePicker();
    },
  );

  document.addEventListener(
    'click',
    (
      event,
    ) => {
      if (
        assigneesPicker &&
        !assigneesPicker.contains(
          event.target as Node,
        )
      ) {
        closeAssigneePicker();
      }
    },
  );

  newButton?.addEventListener(
    'click',
    openCreateModal,
  );

  modalClose?.addEventListener(
    'click',
    closeModal,
  );

  modalCancel?.addEventListener(
    'click',
    closeModal,
  );

  modalBackdrop?.addEventListener(
    'click',
    closeModal,
  );

  detailsClose?.addEventListener(
    'click',
    closeDetailsModal,
  );

  detailsBackdrop?.addEventListener(
    'click',
    closeDetailsModal,
  );

  detailsEdit?.addEventListener(
    'click',
    () => {
      if (!detailsItem) {
        return;
      }

      const item =
        detailsItem;

      closeDetailsModal();
      openEditModal(item);
    },
  );

  progressInput?.addEventListener(
    'input',
    syncProgress,
  );

  statusInput?.addEventListener(
    'change',
    () => {
      if (
        statusInput.value ===
        'done'
      ) {
        if (stageInput) {
          stageInput.value =
            'done';
        }

        syncStageVisualFromSelect();

        if (progressInput) {
          progressInput.value =
            '100';
        }

        syncProgress();
      }
    },
  );

  filterButtons.forEach(
    (button) => {
      button.addEventListener(
        'click',
        () => {
          activeFilter =
            (
              button.dataset.filter ??
              'all'
            ) as typeof activeFilter;

          filterButtons.forEach(
            (item) => {
              item.classList.toggle(
                'active',
                item === button,
              );
            },
          );

          renderRoadmap();
        },
      );
    },
  );

  form?.addEventListener(
    'submit',
    async (event) => {
      event.preventDefault();

      if (
        !session ||
        !titleInput?.value.trim()
      ) {
        showFormStatus(
          'Informe um título.',
          'error',
        );

        return;
      }

      const editingId =
        itemId?.value ??
        '';

      if (saveButton) {
        saveButton.disabled =
          true;

        saveButton.textContent =
          'Salvando...';
      }

      try {
        const input =
          getFormInput();

        let savedItemId =
          editingId;

        if (editingId) {
          await updateRoadmapItem(
            editingId,
            input,
          );
        } else {
          const createdItem:
            any =
              await createRoadmapItem(
                input,
                session.user.id,
              );

          savedItemId =
            createdItem?.id ??
            '';
        }

        if (
          !savedItemId
        ) {
          const refreshedItems =
            await getRoadmapItems();

          const matching =
            refreshedItems.find(
              (
                candidate,
              ) =>
                candidate.title ===
                  input.title &&
                candidate.stage ===
                  input.stage &&
                candidate.status ===
                  input.status &&
                candidate.assignee_user_id ===
                  input.assignee_user_id,
            );

          savedItemId =
            matching?.id ??
            '';
        }

        if (
          savedItemId
        ) {
          await setRoadmapAssignees(
            savedItemId,
            selectedAssigneeIds,
          );
        }

        await loadRoadmap();
        closeModal();
      } catch (error) {
        console.error(
          '[EVRYLUX] Erro ao salvar item:',
          error,
        );

        showFormStatus(
          'Não foi possível salvar o item.',
          'error',
        );
      } finally {
        if (saveButton) {
          saveButton.disabled =
            false;

          saveButton.textContent =
            editingId
              ? 'Salvar alterações'
              : 'Criar item';
        }
      }
    },
  );

  deleteButton?.addEventListener(
    'click',
    async () => {
      const id =
        itemId?.value;

      if (!id) {
        return;
      }

      if (
        !window.confirm(
          'Excluir este item do roadmap?',
        )
      ) {
        return;
      }

      deleteButton.disabled =
        true;

      try {
        await deleteRoadmapItem(id);

        await loadRoadmap();
        closeModal();
      } catch (error) {
        console.error(
          '[EVRYLUX] Erro ao excluir item:',
          error,
        );

        showFormStatus(
          'Não foi possível excluir o item.',
          'error',
        );
      } finally {
        deleteButton.disabled =
          false;
      }
    },
  );

  document.addEventListener(
    'keydown',
    (event) => {
      if (
        event.key === 'Escape'
      ) {
        if (
          detailsModal &&
          !detailsModal.classList.contains(
            'member-hidden',
          )
        ) {
          closeDetailsModal();
          return;
        }

        if (
          modal &&
          !modal.classList.contains(
            'member-hidden',
          )
        ) {
          closeModal();
        }
      }
    },
  );

  if (session) {
    await loadRoadmap();

    loading?.classList.add(
      'member-hidden',
    );

    content?.classList.remove(
      'member-hidden',
    );

    const requestedTaskId =
      new URLSearchParams(
        window.location.search,
      ).get(
        'task',
      );

    if (
      requestedTaskId
    ) {
      const requestedTask =
        roadmapItems.find(
          (
            item,
          ) =>
            item.id ===
            requestedTaskId,
        );

      if (
        requestedTask
      ) {
        window.setTimeout(
          () => {
            openDetailsModal(
              requestedTask,
            );
          },
          80,
        );
      }
    }
  }
</script>


<style is:global>
  /* ==========================================================
     GARANTIAS GLOBAIS
     ========================================================== */

  .member-hidden {
    display: none !important;
  }

  body.roadmap-modal-open,
  body.roadmap-details-open {
    overflow: hidden !important;
  }

  /* ==========================================================
     MODAIS
     ========================================================== */

  .roadmap-modal,
  .roadmap-details-modal {
    position: fixed !important;
    inset: 0 !important;
    z-index: 9999 !important;
    display: grid;
    place-items: center;
    padding: 20px;
    isolation: isolate;
  }

  .roadmap-modal-backdrop,
  .roadmap-details-backdrop {
    position: absolute !important;
    inset: 0 !important;
    width: 100%;
    height: 100%;
    border: 0;
    padding: 0;
    background: rgba(18,27,20,.34);
    backdrop-filter: blur(9px);
    -webkit-backdrop-filter: blur(9px);
    cursor: default;
  }

  .roadmap-modal-card,
  .roadmap-details-card {
    width: min(760px, calc(100vw - 32px));
    max-height: calc(100vh - 40px);
    position: relative;
    z-index: 1;
    overflow-y: auto;
    overscroll-behavior: contain;
    border: 1px solid rgba(65,104,62,.13);
    border-radius: 24px;
    background: rgba(253,254,252,.99);
    box-shadow:
      0 36px 110px rgba(23,32,25,.24);
  }

  .roadmap-modal-card {
    padding: 22px;
  }

  .roadmap-details-card {
    width: min(700px, calc(100vw - 32px));
    padding: 26px;
  }

  .roadmap-modal-head,
  .roadmap-details-head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 18px;
  }

  .roadmap-modal-kicker,
  .roadmap-details-kicker {
    display: block;
    color: var(--primary-deep, #3b6939);
    font-size: 9px;
    font-weight: 850;
    letter-spacing: .08em;
    text-transform: uppercase;
  }

  .roadmap-modal-head h2,
  .roadmap-details-head h2 {
    margin: 6px 0 0;
    color: var(--text, #172019);
    font-size: 28px;
    line-height: 1.05;
    letter-spacing: -.045em;
  }

  .roadmap-details-heading > p {
    max-width: 600px;
    margin: 10px 0 0;
    color: var(--text-soft, #68746b);
    font-size: 13px;
    line-height: 1.65;
    white-space: pre-wrap;
    overflow-wrap: anywhere;
  }

  .roadmap-modal-close,
  .roadmap-details-close {
    width: 38px;
    height: 38px;
    flex: 0 0 auto;
    display: grid;
    place-items: center;
    border: 1px solid rgba(65,104,62,.11);
    border-radius: 11px;
    background: #f7faf5;
    color: var(--text, #172019);
    font: inherit;
    font-size: 21px;
    cursor: pointer;
  }

  .roadmap-modal-close:hover,
  .roadmap-details-close:hover {
    background: #edf5e9;
  }

  /* ==========================================================
     FORMULÁRIO
     ========================================================== */

  .roadmap-form {
    margin-top: 20px;
    display: grid;
    gap: 12px;
  }

  .roadmap-main-grid,
  .roadmap-form-grid,
  .roadmap-visual-grid,
  .roadmap-progress-row {
    display: grid;
    gap: 11px;
  }

  .roadmap-main-grid {
    grid-template-columns: .9fr 1.1fr;
  }

  .roadmap-form-grid,
  .roadmap-visual-grid {
    grid-template-columns: repeat(2, minmax(0,1fr));
  }

  .roadmap-progress-row {
    grid-template-columns: .88fr 1.12fr;
    align-items: stretch;
  }

  .roadmap-field {
    min-width: 0;
    display: grid;
    gap: 6px;
  }

  .roadmap-field label,
  .roadmap-control-head label,
  .roadmap-control-head > span {
    color: var(--text, #172019);
    font-size: 10px;
    font-weight: 800;
  }

  .roadmap-field input:not([type="range"]),
  .roadmap-field textarea,
  .roadmap-field select {
    width: 100%;
    box-sizing: border-box;
    border: 1px solid rgba(65,104,62,.12);
    border-radius: 11px;
    background: #fff;
    color: var(--text, #172019);
    font: inherit;
    font-size: 12px;
    outline: none;
  }

  .roadmap-field input:not([type="range"]),
  .roadmap-field select {
    min-height: 42px;
    padding: 0 12px;
  }

  .roadmap-field textarea {
    min-height: 76px;
    padding: 10px 12px;
    resize: vertical;
    line-height: 1.55;
  }

  .roadmap-title-field input {
    min-height: 58px !important;
    font-size: 14px !important;
    font-weight: 700;
  }

  .roadmap-field input:focus,
  .roadmap-field textarea:focus,
  .roadmap-field select:focus {
    border-color: rgba(95,157,89,.46);
    box-shadow: 0 0 0 4px rgba(159,211,149,.12);
  }

  .roadmap-visual-control,
  .roadmap-progress-field {
    min-width: 0;
    padding: 11px 12px 10px;
    border: 1px solid rgba(65,104,62,.1);
    border-radius: 14px;
    background: #f6faf3;
  }

  .roadmap-control-head,
  .roadmap-card-progress-head,
  .roadmap-details-progress-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
  }

  .roadmap-control-head {
    margin-bottom: 8px;
  }

  .roadmap-control-head strong {
    color: var(--primary-deep, #3b6939);
    font-size: 11px;
  }

  .roadmap-range {
    width: 100%;
    accent-color: var(--primary-strong, #5f9d59);
  }

  .roadmap-range-labels {
    margin-top: 3px;
    display: grid;
    color: var(--text-soft, #68746b);
    font-size: 8px;
  }

  .roadmap-stage-labels {
    grid-template-columns: repeat(4, 1fr);
  }

  .roadmap-priority-labels {
    grid-template-columns: repeat(3, 1fr);
  }

  .roadmap-range-labels span {
    text-align: center;
  }

  .roadmap-range-labels span:first-child {
    text-align: left;
  }

  .roadmap-range-labels span:last-child {
    text-align: right;
  }

  .roadmap-hidden-select {
    position: absolute !important;
    width: 1px !important;
    height: 1px !important;
    overflow: hidden !important;
    opacity: 0 !important;
    pointer-events: none !important;
  }

  .roadmap-notes-field textarea {
    min-height: 76px;
  }

  .roadmap-form-status {
    min-height: 16px;
    font-size: 10px;
  }

  .roadmap-form-status.error {
    color: #a24537;
  }

  .roadmap-form-status.success {
    color: var(--primary-deep, #3b6939);
  }

  .roadmap-modal-actions {
    padding-top: 4px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
  }

  .roadmap-modal-actions-right {
    margin-left: auto;
    display: flex;
    gap: 8px;
  }

  .roadmap-button {
    min-height: 40px;
    padding: 0 14px;
    border-radius: 11px;
    font: inherit;
    font-size: 11px;
    font-weight: 800;
    cursor: pointer;
  }

  .roadmap-button:disabled {
    opacity: .58;
    cursor: not-allowed;
  }

  .roadmap-button-primary {
    border: 1px solid rgba(95,157,89,.2);
    background: var(--primary-deep, #3b6939);
    color: #fff;
  }

  .roadmap-button-secondary {
    border: 1px solid rgba(65,104,62,.11);
    background: #f5f8f3;
    color: var(--text, #172019);
  }

  .roadmap-button-danger {
    border: 1px solid rgba(162,69,55,.14);
    background: #fff0ed;
    color: #9a4638;
  }

  /* ==========================================================
     CARD DO ROADMAP
     ========================================================== */

  .roadmap-item-card {
    width: 100%;
    box-sizing: border-box;
    padding: 15px;
    display: block;
    overflow: hidden;
    border: 1px solid rgba(65,104,62,.10);
    border-radius: 15px;
    background: rgba(255,255,255,.92);
    box-shadow: 0 8px 24px rgba(23,32,25,.035);
    cursor: pointer;
    transition:
      transform .16s ease,
      border-color .16s ease,
      box-shadow .16s ease;
  }

  .roadmap-item-card:hover {
    transform: translateY(-2px);
    border-color: rgba(95,157,89,.24);
    box-shadow: 0 14px 32px rgba(23,32,25,.07);
  }

  .roadmap-item-card:focus-visible {
    outline: none;
    border-color: rgba(95,157,89,.46);
    box-shadow: 0 0 0 4px rgba(159,211,149,.13);
  }

  .roadmap-card-top {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 8px;
  }

  .roadmap-card-badges,
  .roadmap-details-badges {
    display: flex;
    align-items: center;
    gap: 5px;
    flex-wrap: wrap;
  }

  .roadmap-pill {
    min-height: 23px;
    padding: 0 8px;
    display: inline-flex;
    align-items: center;
    border-radius: 999px;
    font-size: 8px;
    line-height: 1;
    font-weight: 800;
  }

  .roadmap-priority[data-priority="low"] {
    background: #f1f5f0;
    color: #657065;
  }

  .roadmap-priority[data-priority="medium"] {
    background: #fff5d9;
    color: #866819;
  }

  .roadmap-priority[data-priority="high"] {
    background: #ffe7e2;
    color: #98493b;
  }

  .roadmap-status[data-status="todo"] {
    background: #eef2ec;
    color: #657065;
  }

  .roadmap-status[data-status="in_progress"] {
    background: #e7f2ff;
    color: #38658e;
  }

  .roadmap-status[data-status="blocked"] {
    background: #fff0ed;
    color: #974536;
  }

  .roadmap-status[data-status="done"] {
    background: #e8f5e4;
    color: #3b6939;
  }

  .roadmap-card-open {
    color: var(--text-soft, #68746b);
    font-size: 12px;
    opacity: .72;
  }

  .roadmap-item-card h3 {
    margin: 13px 0 0;
    color: var(--text, #172019);
    font-size: 16px;
    line-height: 1.25;
    letter-spacing: -.025em;
    overflow-wrap: anywhere;
  }

  .roadmap-card-description {
    margin: 7px 0 0;
    color: var(--text-soft, #68746b);
    font-size: 10px;
    line-height: 1.55;
    overflow-wrap: anywhere;
    display: -webkit-box;
    -webkit-line-clamp: 4;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .roadmap-card-assignee {
    margin-top: 15px;
    padding-top: 12px;
    display: flex;
    align-items: center;
    gap: 9px;
    border-top: 1px solid rgba(65,104,62,.07);
  }

  .roadmap-card-avatar,
  .roadmap-details-avatar {
    flex: 0 0 auto;
    display: grid;
    place-items: center;
    overflow: hidden;
    border: 1px solid rgba(65,104,62,.10);
    border-radius: 50%;
    background: #f7faf6;
    box-shadow:
      0 5px 14px
      rgba(54,92,51,.055);
  }

  .roadmap-profile-avatar-image {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
    border-radius: inherit;
  }

  .roadmap-card-avatar {
    width: 32px;
    height: 32px;
  }

  .roadmap-details-avatar {
    width: 42px;
    height: 42px;
  }

  .roadmap-card-assignee > div {
    min-width: 0;
  }

  .roadmap-card-assignee span:not(.roadmap-card-avatar) {
    display: block;
    color: var(--text-soft, #68746b);
    font-size: 8px;
  }

  .roadmap-card-assignee strong {
    max-width: 100%;
    margin-top: 2px;
    display: block;
    overflow: hidden;
    color: var(--text, #172019);
    font-size: 9px;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .roadmap-card-progress {
    margin-top: 12px;
  }

  .roadmap-card-progress-head {
    margin-bottom: 6px;
    color: var(--text-soft, #68746b);
    font-size: 8px;
  }

  .roadmap-card-progress-head strong {
    color: var(--text, #172019);
  }

  .roadmap-card-progress-track,
  .roadmap-details-progress-track {
    width: 100%;
    overflow: hidden;
    border-radius: 999px;
    background: rgba(65,104,62,.10);
  }

  .roadmap-card-progress-track {
    height: 5px;
  }

  .roadmap-card-progress-fill,
  #roadmap-details-progress-fill {
    width: 0;
    height: 100%;
    display: block;
    border-radius: inherit;
    background: var(--primary-strong, #5f9d59);
    transition: width .22s ease;
  }

  /* ==========================================================
     DETALHES
     ========================================================== */

  .roadmap-details-badges {
    margin-top: 12px;
  }

  .roadmap-details-grid {
    margin-top: 24px;
    display: grid;
    grid-template-columns: repeat(2, minmax(0,1fr));
    gap: 10px;
  }

  .roadmap-details-info {
    min-width: 0;
    min-height: 72px;
    padding: 13px;
    display: flex;
    align-items: center;
    gap: 10px;
    border: 1px solid rgba(65,104,62,.09);
    border-radius: 13px;
    background: #f7faf5;
  }

  .roadmap-details-info > div {
    min-width: 0;
  }

  .roadmap-details-info span:not(.roadmap-details-avatar) {
    display: block;
    color: var(--text-soft, #68746b);
    font-size: 8px;
    font-weight: 700;
  }

  .roadmap-details-info strong {
    margin-top: 4px;
    display: block;
    color: var(--text, #172019);
    font-size: 11px;
    overflow-wrap: anywhere;
  }

  .roadmap-details-progress {
    margin-top: 14px;
    padding: 14px;
    border: 1px solid rgba(65,104,62,.09);
    border-radius: 13px;
    background: #f7faf5;
  }

  .roadmap-details-progress-head {
    margin-bottom: 9px;
    font-size: 10px;
  }

  .roadmap-details-progress-head span {
    color: var(--text-soft, #68746b);
  }

  .roadmap-details-progress-track {
    height: 7px;
  }

  .roadmap-details-notes {
    margin-top: 14px;
    padding: 14px;
    border: 1px solid rgba(65,104,62,.09);
    border-radius: 13px;
    background: #fff;
  }

  .roadmap-details-notes > span {
    color: var(--text-soft, #68746b);
    font-size: 8px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: .06em;
  }

  .roadmap-details-notes p {
    margin: 7px 0 0;
    color: var(--text, #172019);
    font-size: 11px;
    line-height: 1.65;
    white-space: pre-wrap;
    overflow-wrap: anywhere;
  }

  .roadmap-details-actions {
    margin-top: 18px;
    display: flex;
    justify-content: flex-end;
  }

  /* ==========================================================
     RESPONSIVO MODAIS
     ========================================================== */

  @media (max-width: 720px) {
    .roadmap-modal,
    .roadmap-details-modal {
      padding: 8px;
    }

    .roadmap-modal-card,
    .roadmap-details-card {
      width: 100%;
      max-height: calc(100vh - 16px);
      padding: 18px;
      border-radius: 18px;
    }

    .roadmap-main-grid,
    .roadmap-form-grid,
    .roadmap-visual-grid,
    .roadmap-progress-row,
    .roadmap-details-grid {
      grid-template-columns: 1fr;
    }

    .roadmap-modal-actions {
      align-items: stretch;
      flex-direction: column;
    }

    .roadmap-modal-actions-right {
      width: 100%;
      margin-left: 0;
    }

    .roadmap-modal-actions-right .roadmap-button {
      flex: 1;
    }

    .roadmap-details-actions .roadmap-button {
      width: 100%;
    }
  }
</style>

<style>
  /* ==========================================================
     PÁGINA
     ========================================================== */

  .roadmap-page {
    min-height: 100vh;
  }

  .roadmap-toolbar {
    margin-top: 28px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    flex-wrap: wrap;
  }

  .roadmap-filter-row {
    display: flex;
    align-items: center;
    gap: 7px;
    flex-wrap: wrap;
  }

  .roadmap-filter,
  .roadmap-new-button {
    transition:
      transform .15s ease,
      border-color .15s ease,
      background .15s ease;
  }

  .roadmap-filter {
    min-height: 38px;
    padding: 0 13px;
    border: 1px solid rgba(65,104,62,.11);
    border-radius: 11px;
    background: rgba(255,255,255,.72);
    color: var(--text-soft, #68746b);
    font: inherit;
    font-size: 11px;
    font-weight: 780;
    cursor: pointer;
  }

  .roadmap-filter:hover {
    transform: translateY(-1px);
    border-color: rgba(95,157,89,.22);
  }

  .roadmap-filter.active {
    border-color: rgba(95,157,89,.18);
    background: var(--primary-soft, #dff1da);
    color: var(--primary-deep, #3b6939);
  }

  .roadmap-new-button {
    min-height: 42px;
    padding: 0 15px;
    display: inline-flex;
    align-items: center;
    gap: 7px;
    border: 1px solid var(--primary-deep, #3b6939);
    border-radius: 12px;
    background: var(--primary-deep, #3b6939);
    color: #fff;
    font: inherit;
    font-size: 11px;
    font-weight: 820;
    cursor: pointer;
  }

  .roadmap-new-button:hover {
    transform: translateY(-1px);
  }

  .roadmap-error {
    margin-top: 18px;
    padding: 14px 16px;
    border: 1px solid rgba(162,69,55,.15);
    border-radius: 14px;
    background: #fff0ed;
    color: #923f32;
    font-size: 11px;
  }

  .roadmap-board {
    margin-top: 24px;
    display: grid;
    grid-template-columns:
      repeat(4, minmax(0,1fr));
    gap: 12px;
    align-items: start;
  }

  .roadmap-column {
    min-width: 0;
    padding: 14px;
    overflow: hidden;
    border: 1px solid rgba(65,104,62,.10);
    border-radius: 18px;
    background: rgba(255,255,255,.48);
  }

  .roadmap-column-head {
    min-height: 58px;
    margin-bottom: 12px;
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 12px;
  }

  .roadmap-column-kicker {
    display: block;
    margin-bottom: 4px;
    color: var(--text-soft, #68746b);
    font-size: 8px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: .08em;
  }

  .roadmap-column h2 {
    margin: 0;
    color: var(--text, #172019);
    font-size: 21px;
    line-height: 1.05;
    letter-spacing: -.035em;
  }

  .roadmap-column-count {
    min-width: 30px;
    height: 30px;
    flex: 0 0 auto;
    display: grid;
    place-items: center;
    border-radius: 10px;
    background: #f1f5ef;
    color: var(--text-soft, #68746b);
    font-size: 10px;
    font-weight: 850;
  }

  .roadmap-column-list {
    min-width: 0;
    min-height: 155px;
    max-height: calc(100vh - 360px);
    padding-right: 3px;
    display: grid;
    align-content: start;
    gap: 10px;
    overflow-y: auto;
    overflow-x: hidden;
    scrollbar-width: thin;
  }

  :global(.roadmap-empty) {
    min-height: 110px;
    padding: 15px 12px;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    gap: 5px;
    border: 1px dashed rgba(65,104,62,.10);
    border-radius: 13px;
    background: rgba(247,250,245,.55);
  }

  :global(.roadmap-empty strong) {
    display: block;
    color: var(--text, #172019);
    font-size: 10px;
    line-height: 1.4;
  }

  :global(.roadmap-empty span) {
    display: block;
    color: var(--text-soft, #68746b);
    font-size: 9px;
    line-height: 1.5;
  }

  @media (max-width: 1120px) {
    .roadmap-board {
      grid-template-columns:
        repeat(2, minmax(0,1fr));
    }

    .roadmap-column-list {
      max-height: 460px;
    }
  }

  @media (max-width: 650px) {
    .roadmap-toolbar {
      align-items: stretch;
      flex-direction: column;
    }

    .roadmap-filter-row {
      width: 100%;
    }

    .roadmap-filter {
      flex: 1;
    }

    .roadmap-new-button {
      justify-content: center;
      width: 100%;
    }

    .roadmap-board {
      grid-template-columns: 1fr;
    }

    .roadmap-column-list {
      max-height: 460px;
    }
  }
</style>


<style is:global>
  /* ==========================================================
     ROADMAP — MODO COMPACTO
     Mais itens visíveis por coluna sem perder legibilidade.
     ========================================================== */

  .roadmap-column {
    padding: 11px !important;
    border-radius: 16px !important;
  }

  .roadmap-column-head {
    min-height: 48px !important;
    margin-bottom: 8px !important;
  }

  .roadmap-column-kicker {
    margin-bottom: 3px !important;
    font-size: 7px !important;
  }

  .roadmap-column h2 {
    font-size: 18px !important;
  }

  .roadmap-column-count {
    min-width: 26px !important;
    height: 26px !important;
    border-radius: 9px !important;
    font-size: 9px !important;
  }

  .roadmap-column-list {
    min-height: 135px !important;
    max-height: calc(100vh - 285px) !important;
    padding-right: 2px !important;
    gap: 7px !important;
  }

  .roadmap-item-card {
    min-height: 0 !important;
    padding: 11px !important;
    border-radius: 13px !important;
    box-shadow: 0 5px 18px rgba(23,32,25,.03) !important;
  }

  .roadmap-card-top {
    gap: 5px !important;
  }

  .roadmap-card-badges,
  .roadmap-details-badges {
    gap: 4px !important;
  }

  .roadmap-pill {
    min-height: 20px !important;
    padding: 0 7px !important;
    font-size: 7px !important;
  }

  .roadmap-card-open {
    font-size: 10px !important;
  }

  .roadmap-item-card h3 {
    margin-top: 9px !important;
    font-size: 14px !important;
    line-height: 1.2 !important;
  }

  .roadmap-card-description {
    margin-top: 5px !important;
    font-size: 9px !important;
    line-height: 1.45 !important;
    -webkit-line-clamp: 2 !important;
  }

  .roadmap-card-assignee {
    margin-top: 9px !important;
    padding-top: 8px !important;
    gap: 7px !important;
  }

  .roadmap-card-avatar {
    width: 27px !important;
    height: 27px !important;
    border-radius: 50% !important;
  }

  .roadmap-card-assignee span:not(.roadmap-card-avatar) {
    font-size: 7px !important;
  }

  .roadmap-card-assignee strong {
    margin-top: 1px !important;
    font-size: 8px !important;
  }

  .roadmap-card-progress {
    margin-top: 8px !important;
  }

  .roadmap-card-progress-head {
    margin-bottom: 4px !important;
    font-size: 7px !important;
  }

  .roadmap-card-progress-track {
    height: 4px !important;
  }

  .roadmap-empty {
    min-height: 92px !important;
    padding: 11px 10px !important;
    gap: 3px !important;
    border-radius: 11px !important;
  }

  .roadmap-empty strong {
    font-size: 9px !important;
  }

  .roadmap-empty span {
    font-size: 8px !important;
  }

  @media (min-width: 1121px) {
    .roadmap-board {
      gap: 10px !important;
    }
  }

  @media (max-width: 1120px) {
    .roadmap-column-list {
      max-height: 520px !important;
    }
  }

  @media (max-width: 650px) {
    .roadmap-item-card {
      padding: 12px !important;
    }

    .roadmap-card-description {
      -webkit-line-clamp: 3 !important;
    }

    .roadmap-column-list {
      max-height: 560px !important;
    }
  }
</style>


<style is:global>
  /* ==========================================================
     ROADMAP — CARDS RECOLHÍVEIS
     Fechado: somente o título.
     Aberto: descrição, status, responsável e progresso.
     ========================================================== */

  .roadmap-item-collapsible {
    padding: 0 !important;
    overflow: hidden !important;
    cursor: default;
  }

  .roadmap-card-summary {
    min-height: 48px;
    padding: 10px 12px;
    display: grid;
    grid-template-columns: minmax(0,1fr) 22px;
    align-items: center;
    gap: 8px;
  }

  .roadmap-card-summary h3,
  .roadmap-item-collapsible h3 {
    margin: 0 !important;
    min-width: 0;
    color: var(--text, #172019);
    font-size: 13px !important;
    line-height: 1.25 !important;
    font-weight: 800;
    letter-spacing: -.02em;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }


  .roadmap-card-toggle {
    width: 28px;
    height: 28px;
    padding: 0;
    display: grid;
    place-items: center;
    justify-self: end;
    flex: 0 0 auto;
    border: 1px solid rgba(65,104,62,.10);
    border-radius: 9px;
    background: rgba(247,250,245,.88);
    color: var(--text-soft, #68746b);
    cursor: pointer;
    transition:
      transform .16s ease,
      background .16s ease,
      border-color .16s ease,
      color .16s ease;
  }

  .roadmap-card-toggle:hover {
    background: rgba(223,241,218,.75);
    border-color: rgba(95,157,89,.20);
    color: var(--primary-deep, #3b6939);
  }

  .roadmap-card-toggle:focus-visible {
    outline: none;
    box-shadow:
      0 0 0 3px
      rgba(95,157,89,.12);
  }

  .roadmap-card-toggle svg {
    width: 16px;
    height: 16px;
    display: block;
    transition:
      transform .18s ease;
  }

  .roadmap-item-collapsible:not(.is-expanded)
  .roadmap-card-toggle svg {
    transform: rotate(180deg);
  }

  .roadmap-card-expanded {
    display: none;
    padding: 0 12px 12px;
    border-top: 1px solid rgba(65,104,62,.07);
  }

  .roadmap-item-collapsible.is-expanded .roadmap-card-expanded {
    display: block;
    animation: roadmap-card-reveal .16s ease;
  }

  @keyframes roadmap-card-reveal {
    from {
      opacity: 0;
      transform: translateY(-3px);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .roadmap-card-expanded .roadmap-card-badges {
    margin-top: 10px;
  }

  .roadmap-card-expanded .roadmap-card-description {
    margin-top: 8px !important;
    display: block !important;
    overflow: visible !important;
    -webkit-line-clamp: unset !important;
    -webkit-box-orient: initial !important;
    font-size: 9px !important;
    line-height: 1.5 !important;
  }

  .roadmap-card-expanded .roadmap-card-assignee {
    margin-top: 10px !important;
    padding-top: 9px !important;
  }

  .roadmap-card-expanded .roadmap-card-progress {
    margin-top: 9px !important;
  }

  .roadmap-card-details-button {
    width: 100%;
    min-height: 32px;
    margin-top: 11px;
    border: 1px solid rgba(65,104,62,.10);
    border-radius: 9px;
    background: rgba(239,248,235,.62);
    color: var(--primary-deep, #3b6939);
    font: inherit;
    font-size: 8px;
    font-weight: 850;
    cursor: pointer;
  }

  .roadmap-card-details-button:hover {
    background: rgba(223,241,218,.9);
  }

  .roadmap-column-list {
    gap: 6px !important;
  }

  /* Com os cards fechados, várias tarefas cabem na coluna. */
  .roadmap-item-collapsible:not(.is-expanded) {
    min-height: 48px !important;
    box-shadow: 0 4px 14px rgba(23,32,25,.025) !important;
  }

  .roadmap-item-collapsible.is-expanded {
    box-shadow: 0 10px 28px rgba(23,32,25,.06) !important;
  }

  @media (max-width: 650px) {
    .roadmap-card-summary {
      min-height: 50px;
      padding: 11px 12px;
    }

    .roadmap-card-summary h3,
    .roadmap-item-collapsible h3 {
      font-size: 13px !important;
    }
  }
</style>

<style is:global>
  /* ==========================================================
     EVRYLUX ROADMAP v2026.09.22
     Estado recolhido + avatar real do perfil
     ========================================================== */

  .roadmap-item-card.roadmap-item-collapsible {
    padding: 0 !important;
  }

  .roadmap-item-card.roadmap-item-collapsible
  .roadmap-card-expanded {
    display: none !important;
  }

  .roadmap-item-card.roadmap-item-collapsible.is-expanded
  .roadmap-card-expanded {
    display: block !important;
  }

  .roadmap-item-card.roadmap-item-collapsible:not(.is-expanded) {
    min-height: 48px !important;
    height: auto !important;
  }

  .roadmap-item-card.roadmap-item-collapsible:not(.is-expanded)
  .roadmap-card-summary {
    min-height: 48px !important;
  }

  .roadmap-card-avatar,
  .roadmap-details-avatar {
    background-image: none !important;
    border-radius: 50% !important;
  }

  .roadmap-card-avatar .roadmap-profile-avatar-image,
  .roadmap-details-avatar .roadmap-profile-avatar-image {
    width: 100% !important;
    height: 100% !important;
    min-width: 100% !important;
    min-height: 100% !important;
    max-width: 100% !important;
    max-height: 100% !important;
    object-fit: cover !important;
    border-radius: 50% !important;
  }
</style>


<style is:global>
  /* ==========================================================
     ROADMAP — BOTÃO COMPACTAR / EXPANDIR
     ========================================================== */

  .roadmap-item-card.roadmap-item-collapsible
  .roadmap-card-summary {
    min-height: 48px !important;
    padding: 9px 10px 9px 12px !important;
  }

  .roadmap-item-card.roadmap-item-collapsible:not(.is-expanded)
  .roadmap-card-expanded {
    display: none !important;
  }

  .roadmap-item-card.roadmap-item-collapsible.is-expanded
  .roadmap-card-expanded {
    display: block !important;
  }

  .roadmap-item-card.roadmap-item-collapsible:not(.is-expanded) {
    min-height: 48px !important;
    height: 48px !important;
    border-radius: 13px !important;
  }

  .roadmap-item-card.roadmap-item-collapsible:not(.is-expanded)
  .roadmap-card-summary h3 {
    overflow: hidden !important;
    text-overflow: ellipsis !important;
    white-space: nowrap !important;
  }

  .roadmap-column-list {
    gap: 6px !important;
  }
</style>


<style is:global>
  /* ==========================================================
     ROADMAP — INICIA RECOLHIDO
     ========================================================== */

  .roadmap-item-card.roadmap-item-collapsible:not(.is-expanded) {
    min-height: 48px !important;
    height: 48px !important;
    overflow: hidden !important;
  }

  .roadmap-item-card.roadmap-item-collapsible:not(.is-expanded)
  .roadmap-card-expanded {
    display: none !important;
  }

  .roadmap-item-card.roadmap-item-collapsible:not(.is-expanded)
  .roadmap-card-summary {
    min-height: 48px !important;
    height: 48px !important;
    padding: 8px 10px 8px 12px !important;
    box-sizing: border-box !important;
  }

  .roadmap-item-card.roadmap-item-collapsible:not(.is-expanded)
  .roadmap-card-summary h3 {
    margin: 0 !important;
    overflow: hidden !important;
    text-overflow: ellipsis !important;
    white-space: nowrap !important;
  }

  .roadmap-item-card.roadmap-item-collapsible.is-expanded {
    height: auto !important;
  }

  .roadmap-item-card.roadmap-item-collapsible.is-expanded
  .roadmap-card-expanded {
    display: block !important;
  }
</style>



<style is:global>
  .roadmap-validation-actions{margin-top:11px;display:grid;grid-template-columns:1fr 1fr;gap:6px}
  .roadmap-validation-actions .roadmap-card-details-button{margin-top:0!important}
  .roadmap-card-validation-button{width:100%;min-height:32px;border:1px solid rgba(95,157,89,.16);border-radius:9px;background:var(--primary-deep,#3b6939);color:#fff;font:inherit;font-size:8px;font-weight:850;cursor:pointer}
  .roadmap-card-validation-button.already-sent{background:var(--primary-soft,#dff1da);color:var(--primary-deep,#3b6939)}
  .roadmap-card-validation-button:disabled{opacity:.58}
  @media(max-width:650px){.roadmap-validation-actions{grid-template-columns:1fr}}
</style>
<style is:global>
  /* EVRYLUX PERSISTENT VALIDATION FIX */
  .roadmap-item-collapsible:not(.is-expanded){min-height:48px!important;height:48px!important;overflow:hidden!important}
  .roadmap-item-collapsible:not(.is-expanded) .roadmap-card-expanded{display:none!important}
  .roadmap-item-collapsible:not(.is-expanded) .roadmap-card-summary{min-height:48px!important;height:48px!important;padding:0 10px 0 12px!important;border-bottom:0!important}
  .roadmap-item-collapsible:not(.is-expanded) .roadmap-card-summary h3{margin:0!important;overflow:hidden!important;text-overflow:ellipsis!important;white-space:nowrap!important}
  .roadmap-item-collapsible.is-expanded{height:auto!important;min-height:0!important;overflow:visible!important}
  .roadmap-item-collapsible.is-expanded .roadmap-card-expanded{display:block!important}
  .studio-validation-button{min-height:42px;padding:0 14px;display:inline-flex;align-items:center;justify-content:center;gap:8px;border:1px solid rgba(95,157,89,.18);border-radius:11px;background:linear-gradient(145deg,rgba(239,248,235,.96),rgba(223,241,218,.90));color:var(--primary-deep,#365f35);font-size:9px;font-weight:850;text-decoration:none}
  .studio-validation-button-icon{width:20px;height:20px;display:grid;place-items:center;border-radius:7px;background:rgba(255,255,255,.68)}
  .roadmap-toolbar>.studio-validation-button{margin-left:auto}
  .roadmap-toolbar>.studio-validation-button+#new-roadmap-item{margin-left:0}
  @media(max-width:720px){.roadmap-toolbar>.studio-validation-button,.roadmap-toolbar>#new-roadmap-item{width:100%}}
</style>


<style is:global>
  /* ==========================================================
     EVRYLUX ROADMAP — MÚLTIPLOS RESPONSÁVEIS
     ========================================================== */

  .roadmap-assignees-field {
    position: relative;
  }

  .roadmap-assignees-picker {
    position: relative;
  }

  .roadmap-primary-assignee-select {
    position: absolute !important;
    width: 1px !important;
    height: 1px !important;
    opacity: 0 !important;
    pointer-events: none !important;
  }

  .roadmap-assignees-trigger {
    width: 100%;
    min-height: 42px;
    padding: 0 12px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    border: 1px solid rgba(65,104,62,.15);
    border-radius: 10px;
    background: #fff;
    color: var(--text);
    font: inherit;
    font-size: 10px;
    text-align: left;
    cursor: pointer;
  }

  .roadmap-assignees-trigger[aria-expanded="true"] {
    border-color: rgba(95,157,89,.42);
    box-shadow: 0 0 0 3px rgba(159,211,149,.11);
  }

  .roadmap-assignees-chevron {
    color: var(--text-soft);
    font-size: 10px;
  }

  .roadmap-assignees-menu {
    width: 100%;
    max-height: 230px;
    position: absolute;
    left: 0;
    top: calc(100% + 6px);
    z-index: 80;
    overflow-y: auto;
    padding: 6px;
    box-sizing: border-box;
    border: 1px solid rgba(65,104,62,.13);
    border-radius: 12px;
    background: rgba(255,255,255,.99);
    box-shadow: 0 18px 46px rgba(23,32,25,.14);
  }

  .roadmap-assignee-option {
    width: 100%;
    min-height: 48px;
    padding: 6px 8px;
    display: grid;
    grid-template-columns: 32px minmax(0,1fr) 22px;
    align-items: center;
    gap: 9px;
    border: 0;
    border-radius: 9px;
    background: transparent;
    color: var(--text);
    font: inherit;
    text-align: left;
    cursor: pointer;
  }

  .roadmap-assignee-option:hover,
  .roadmap-assignee-option.selected {
    background: var(--primary-soft);
  }

  .roadmap-assignee-option-avatar {
    width: 30px;
    height: 30px;
    display: block;
    overflow: hidden;
    border-radius: 50%;
    border: 1px solid rgba(65,104,62,.10);
  }

  .roadmap-assignee-option-copy {
    min-width: 0;
  }

  .roadmap-assignee-option-copy strong,
  .roadmap-assignee-option-copy small {
    display: block;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .roadmap-assignee-option-copy strong {
    font-size: 9px;
  }

  .roadmap-assignee-option-copy small {
    margin-top: 2px;
    color: var(--text-soft);
    font-size: 7px;
  }

  .roadmap-assignee-option-check {
    width: 20px;
    height: 20px;
    display: grid;
    place-items: center;
    border: 1px solid rgba(65,104,62,.14);
    border-radius: 6px;
    color: var(--primary-deep);
    font-size: 9px;
    font-weight: 900;
  }

  .roadmap-assignee-option.selected
  .roadmap-assignee-option-check {
    border-color: rgba(95,157,89,.28);
    background: #fff;
  }

  .roadmap-assignees-empty {
    padding: 10px;
    color: var(--text-soft);
    font-size: 8px;
    text-align: center;
  }

  .roadmap-assignees-chips {
    margin-top: 7px;
    display: flex;
    flex-wrap: wrap;
    gap: 5px;
  }

  .roadmap-assignee-chip {
    min-height: 25px;
    padding: 0 8px;
    display: inline-flex;
    align-items: center;
    gap: 6px;
    border: 1px solid rgba(95,157,89,.15);
    border-radius: 999px;
    background: var(--primary-soft);
    color: var(--primary-deep);
    font: inherit;
    font-size: 7px;
    font-weight: 800;
    cursor: pointer;
  }

  .roadmap-assignee-chip b {
    font-size: 10px;
    line-height: 1;
  }

  .roadmap-card-avatar.roadmap-avatar-stack,
  .roadmap-details-avatar.roadmap-avatar-stack {
    width: auto !important;
    min-width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    overflow: visible !important;
    border: 0 !important;
    background: transparent !important;
  }

  .roadmap-stacked-avatar {
    width: 30px;
    height: 30px;
    display: block;
    position: relative;
    overflow: hidden;
    flex: 0 0 30px;
    border: 2px solid #fff;
    border-radius: 50%;
    background: #f5f8f2;
  }

  .roadmap-stacked-avatar + .roadmap-stacked-avatar {
    margin-left: -9px;
  }

  .roadmap-stacked-avatar-more {
    display: grid;
    place-items: center;
    background: var(--primary-soft);
    color: var(--primary-deep);
    font-size: 7px;
    font-weight: 900;
  }

  .roadmap-stacked-avatar
  .roadmap-profile-avatar-image {
    width: 100% !important;
    height: 100% !important;
    object-fit: cover !important;
    border-radius: 50% !important;
  }

  @media (max-width: 650px) {
    .roadmap-assignees-menu {
      max-height: 200px;
    }
  }
</style>


<style is:global>
  /* ==========================================================
     EVRYLUX ROADMAP — MARCAÇÃO "ATRIBUÍDA A MIM"
     Aparece somente quando o usuário logado está entre os
     responsáveis da tarefa.
     ========================================================== */

  .roadmap-card-title-row {
    min-width: 0;
    display: flex;
    align-items: center;
    gap: 9px;
    flex: 1 1 auto;
  }

  .roadmap-card-title-row > h3 {
    min-width: 0;
    margin: 0;
    flex: 0 1 auto;
  }

  .roadmap-my-assignment-marker {
    width: 9px;
    height: 9px;
    flex: 0 0 9px;
    display: inline-block;
    border-radius: 50%;
    background: #20e426;
    border: 1px solid rgba(22, 177, 29, .24);
    box-shadow:
      0 0 0 2px rgba(32, 228, 38, .08),
      0 0 7px rgba(32, 228, 38, .34);
  }

  .roadmap-my-assignment-marker.member-hidden {
    display: none !important;
  }

  @media (max-width: 620px) {
    .roadmap-card-title-row {
      gap: 7px;
    }

    .roadmap-my-assignment-marker {
      width: 8px;
      height: 8px;
      flex-basis: 8px;
    }
  }
</style>

<style is:global>
  /* EVRYLUX ROADMAP MARKER ROBUST CSS V1 */

  .roadmap-card-summary
  [data-role="title"].roadmap-title-assigned-to-me::after {
    content: "";
    width: 8px;
    height: 8px;
    margin-left: 8px;
    display: inline-block;
    vertical-align: middle;
    border-radius: 50%;
    background: #20e426;
    border: 1px solid rgba(18, 165, 25, .30);
    box-shadow:
      0 0 0 2px rgba(32, 228, 38, .08),
      0 0 6px rgba(32, 228, 38, .35);
    transform: translateY(-1px);
  }

  @media (max-width: 620px) {
    .roadmap-card-summary
    [data-role="title"].roadmap-title-assigned-to-me::after {
      width: 7px;
      height: 7px;
      margin-left: 6px;
    }
  }
</style>

